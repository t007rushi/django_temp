1. create 
	py -m virtualenv [env_name]

2.find path for actiavtion of env
	C:\Users\Ghodke\Documents\rushi\projects\django\Scripts\activate

3.to check package list use: pip list the check all dir files using "dir" or make one "mkdir"

4.create new project(main project files IMP: are setting.py and manage.py)
	django-admin startproject project_name

5.create new app(i.e. side feature for main project)
	django-admin startapp app_name

6.to run server 
	python manage.py runserver

GET vs POST
	by default GET - send data through url not secure
	POST - send data in body and not url so use {% csrf_token %}

Template lang
	various templates like .html, first settings.py edit BASE_DIR(root directory(myproject)) , 'templates' folder
	so do code in views.py (as user sees) write functions render data/image + httpresponse on request by user
	next, urls.py set path or include new one by hyperlink

Static files handling
	imp step: setting.py include sttaicfile = [os.path.join(BASE_DIR, "static")]
{% load static %}	
{% static %}



Django models
	also use in program logic of python to understand by django in html file
eg. {% for i in range%}
	from .models import class/function/module

